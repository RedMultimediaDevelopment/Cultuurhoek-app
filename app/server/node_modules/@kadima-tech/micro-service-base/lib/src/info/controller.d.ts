import { FastifyRequest } from 'fastify';
import { IFastifyReply } from '../types';
export interface InfoResponse {
    uptime: number;
    message: string;
    timestamp: number;
    version: string;
}
export declare const infoController: (_: FastifyRequest, reply: IFastifyReply<InfoResponse>) => Promise<void>;
