import { FastifySchema, FastifyPluginCallback } from "fastify";
import { OpenAPIV3 } from "openapi-types";
import { JSONObject } from "@fastify/swagger";
import grpc from "@grpc/grpc-js";
export interface GrpcRoute {
    definition: grpc.ServiceDefinition<grpc.UntypedServiceImplementation>;
    implementation: grpc.UntypedServiceImplementation;
}
export interface ServiceRoute {
    router: FastifyPluginCallback;
    version?: string;
    path?: string;
}
export interface MicroServiceConfig {
    routes: ServiceRoute[];
    grpc?: GrpcRoute[];
    swagger?: Partial<OpenAPIV3.Document>;
    swaggerTransform?: (opts: {
        schema: FastifySchema;
        url: string;
    }) => {
        schema: JSONObject;
        url: string;
    };
    version?: string;
    title?: string;
    description?: string;
    autoDetectHostname?: boolean;
}
export declare const parseRoute: (route: ServiceRoute) => void;
export declare const parseConfiguration: (config: MicroServiceConfig) => void;
