import { requestContext } from '@fastify/request-context';

declare module '@fastify/request-context' {
  interface RequestContextData {
    requestId?: string;
  }
}

export const getRequestIdFromContext = () => requestContext.get('requestId');
