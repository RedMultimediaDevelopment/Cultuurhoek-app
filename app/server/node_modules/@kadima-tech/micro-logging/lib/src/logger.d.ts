import { pino as PinoTypes } from 'pino';
export declare type Logger = PinoTypes.Logger;
export declare type DestinationStream = PinoTypes.DestinationStream;
export declare type LogLevel = PinoTypes.Level;
export interface LoggerOptions {
    level: LogLevel;
    disableRedaction?: boolean;
    redactions?: string[];
    prettyPrint?: boolean;
}
export declare function createLogger(options: LoggerOptions, outputStream?: DestinationStream): Logger;
